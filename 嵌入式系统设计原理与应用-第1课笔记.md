# 嵌入式系统设计原理与应用 · 第 1 课笔记

> 依据课堂录音转写整理；原始转写存在口语化、同音字识别误差，本笔记已归纳重点并补充少量背景知识。
> 适用范围：第一章概述 + ARM/嵌入式微处理器 + 开发流程 + VMware/Ubuntu 环境准备 + Linux 基础指令。

## 一、本课重点速览

| 模块 | 核心内容 | 记忆关键词 |
| --- | --- | --- |
| 嵌入式系统定义 | 专用计算机系统 | 应用为中心、软硬件可裁剪 |
| 系统组成 | 三大部分 | 硬件 + 嵌入式操作系统 + 嵌入式软件 |
| ARM | 三种含义、卖 IP 核 | 公司 / 微处理器 / 技术 |
| 架构对比 | 冯·诺依曼 vs 哈佛 | 总线合一 vs 总线分离 |
| 开发流程 | 定义需求 → 硬件/软件并行 → 验收闭环 | 产品经理、流程图 |
| 设计方法 | 自顶向下为主、自底向上为辅 | 模块化拆解 |
| 环境准备 | VMware + Ubuntu 24 | 打开虚拟机、密码 123456 |
| Linux 指令 | ls、cd、clear、useradd | 命令 空格 对象、大小写敏感 |

## 二、嵌入式系统的定义

**标准定义：** 嵌入式系统是**以应用为中心、以计算机技术为基础、软硬件可裁剪**，适用于应用系统对**功能、可靠性、成本、功耗**等有严格要求的**专用计算机系统**。

**关键词拆解：**

- 以应用为中心：针对具体应用场景定制，不是万能设备。
- 以计算机技术为基础：本质仍是计算机系统。
- 软硬件可裁剪：按需选择硬件资源和软件模块。
- 专用计算机系统：区别于通用 PC。

**补充——嵌入式系统 vs 通用计算机：**

| 维度 | 嵌入式系统 | 通用计算机 |
| --- | --- | --- |
| 定位 | 专用功能 | 通用任务 |
| 软硬件 | 可裁剪、资源受限 | 资源丰富、可扩展 |
| 功耗/体积 | 通常低功耗、小体积 | 功耗和体积较大 |
| 操作系统 | 裁剪定制（Linux、RTOS 等） | Windows、macOS 等 |

## 三、嵌入式系统的组成

课上强调为**三大部分**：

1. **硬件层**
   - 核心是**处理器（芯片）**，如 ARM、8051 内核的 MCU。
   - 外围硬件：内存、硬盘/U 盘、VGA/HDMI、投影等输入输出与存储设备。
2. **嵌入式操作系统**
   - 运行在硬件之上，与 PC 通用 OS（Windows 等）不同，通常更精简、可裁剪。
3. **嵌入式软件**
   - 面向具体应用的用户程序，最终运行在目标硬件上。

**补充：** 常见教材中还有“中间层/板级支持包（BSP）”“应用层”等四层说法。课上按“硬件—操作系统—软件”三层归纳，理解时注意不要混淆：**三部分是大分类，中间层属于对硬件的抽象与适配**。

## 四、ARM 与嵌入式微处理器

### 4.1 全球两大主流架构

| 架构 | 典型设备 |
| --- | --- |
| x86 | PC、笔记本 |
| ARM | 手机、平板、手持设备 |

### 4.2 ARM 的三种含义

1. **公司名称**：ARM 公司。
2. **微处理器的统称/架构**：基于 ARM 内核的处理器。
3. **技术名称**：ARM 架构技术。

### 4.3 ARM 商业模式与芯片产业链

- ARM 公司**不直接造芯片**，主要**研发并授权 IP 核（知识产权核）**。
- IP 核可理解为“芯片的主要架构与核心设计”，不是网络 IP 地址。
- 产业链大致为：**IP 授权 → 芯片设计（合作伙伴）→ 晶圆代工（如台积电）→ 渠道销售 → 终端用户**。
- 芯片/电路模块通常使用**防静电袋**包装：人体静电瞬间放电可达上万伏，可能击穿芯片。

**补充——IP 核分类：** 数字系统课程中常分为**软核、固核、硬核**。软核灵活但需综合实现；硬核性能确定、不可修改；固核介于二者之间。

### 4.4 RISC 与 CISC

- **RISC**：精简指令集计算机（Reduced Instruction Set Computer）。
- **CISC**：复杂指令集计算机（Complex Instruction Set Computer）。
- ARM 采用 **RISC**；传统 PC 的 x86 更偏向 **CISC**。

## 五、常见 ARM 系列与架构对比

### 5.1 ARM 系列

| 系列 | 架构 | 特点 |
| --- | --- | --- |
| ARM7 | 冯·诺依曼架构 | 成熟、批量大、3 级流水线、RISC、低功耗 |
| ARM9 / ARM10 / ARM11 | 哈佛架构 | 程序与数据总线分离，流水线级数更高，处理效率更高 |
| Cortex 系列 | 面向不同应用 | STM32 基于 Cortex-M 内核 |

### 5.2 冯·诺依曼 vs 哈佛架构

**冯·诺依曼架构：**

- 组成：运算器、控制器、存储器、输入设备、输出设备。
- CPU（中央处理器）= 运算器 + 控制器。
- 特点：程序指令与数据**共用同一总线**，可能出现“拥堵”。

**哈佛架构：**

- 程序存储器与数据存储器的**总线相互独立**。
- 指令与数据可并行访问，处理效率更高。
- 类比：教室两个门，一个进、一个出，比单门同时进出更快。

## 六、嵌入式系统开发流程

嵌入式系统开发包括三部分：**总体开发、硬件开发、软件开发**。

1. **系统定义与需求分析（可行性分析）**
   - 岗位对应：产品经理。
   - 职责：在项目立项前与甲方对接、调研需求，明确功能、性能、材质、规格等，避免后期反复改动。
2. **硬件开发 / 软件开发并行**
   - 先明确各自功能与约束，形成方案与合同，再执行。
3. **验收与闭环**
   - 软硬件联调适配；不满足需求则返回修改，形成一个**大循环**。

**流程图是重点：** 顺序步骤用矩形/流程框，条件分支用判断框，对应代码中的 `if`、`if-else`、`switch-case`。

## 七、自顶向下 vs 自底向上设计

| 方法 | 思路 | 类比 | 优缺点 |
| --- | --- | --- | --- |
| 自顶向下（Top-Down） | 把大系统按模块拆分，先总体后细化 | 装修教室：先分讲台、桌椅、照明、投影模块 | 单模块修改不影响其他模块，易维护 |
| 自底向上（Bottom-Up） | 从底层一步步向上搭建 | 盖房子：先打地基再砌墙 | 底层出问题需要层层拆除，修改成本高 |

**结论：** 实际设计中二者**交替结合使用**，但以**自顶向下**为主体。

## 八、交叉编译

- **普通编译**：在电脑（x86）上写代码、编译，并下载/烧录到目标板。
- **交叉编译**：在**宿主机**（如 x86 的 PC）上，借助**交叉编译器**，生成能在**目标机**（如 ARM）上运行的目标代码。
- 原因：x86 平台编译出的程序不能直接运行在 ARM 架构上。

**补充：** 常见交叉编译链如 `arm-linux-gnueabihf-gcc` 等，用于生成 ARM Linux 可执行文件。

## 九、环境准备：VMware + Ubuntu

### 9.1 文件与安装

- 提供的文件夹通常包含：**Ubuntu 24 完整系统（非 ISO 镜像）**、**VMware 安装程序**、**激活码**。
- VMware 安装：常规“下一步”，最后输入激活码即可。

### 9.2 VMware 三个主要选项

1. **创建虚拟机**：从 ISO 镜像文件安装全新系统。
2. **打开虚拟机**：打开现成的完整系统（本课程使用）。
3. **远程连接服务器**：本课程暂不涉及。

### 9.3 虚拟机资源分配

- 示例配置：内存 4 GB、CPU 2 核、硬盘 20 GB。
- 原则：虚拟机占用本机资源，**不要分配过多**，否则本机会卡顿。

### 9.4 开关机状态

| 操作 | 含义 |
| --- | --- |
| 关机 | 关闭虚拟机操作系统 |
| 挂起 | 锁定当前状态，不丢失运行现场，下次可直接继续 |
| 重启 | 重启虚拟机 |

### 9.5 Ubuntu 登录与网络

- 账户密码：**123456**（学习阶段使用）。
- 三种网络模式：
  - **桥接模式（Bridged）**：虚拟机像局域网中独立主机。
  - **NAT（网络地址转换）**：通过宿主机共享上网。
  - **仅主机（Host-Only）**：只与宿主机通信。

## 十、Linux/Ubuntu 基本认知

- **一切皆文件**：普通文件、文件夹、设备驱动等在 Linux 中都视为文件。
- **根目录 `/`**：所有文件的起点。
- 常见目录：

| 目录 | 作用 |
| --- | --- |
| `/bin` | 可执行二进制文件 |
| `/boot` | 启动相关文件 |
| `/dev` | 设备文件 |
| `/etc` | 配置文件 |
| `/home` | 用户主目录（日常操作主要在这里） |
| `/mnt` | 挂载目录 |
| `/tmp` | 临时文件 |

- Windows 用盘符（C、D、E…），Linux 用**单一根目录 + 目录树**。
- 终端操作：右键“在终端打开”；放大字体 `Ctrl+Shift+=`，缩小 `Ctrl+-`。
- 实验截图建议把终端背景调成**白色**，避免打印时漆黑一片。

## 十一、Linux 指令基础

- 指令格式：`命令 + 空格 + 操作对象`。命令与对象之间**必须有空格**。
- **区分大小写**：`ls` 有效，`LS` 无效。
- **Shell**：既是**命令解释器**，也是一门**脚本编程语言**；Linux 另有内置命令。
- 常用分类：文件管理、用户管理、打包压缩、磁盘管理、网络管理、进程管理等。

| 指令 | 作用 |
| --- | --- |
| `ls` | 列出当前目录内容 |
| `cd /home` | 切换到 /home 目录 |
| `clear` | 清屏 |
| `sudo useradd 用户名` | 以管理员权限添加用户 |

### 11.1 useradd 实操要点

- 添加用户需要 **root/管理员权限**，普通用户需用 `sudo`。
- 输入密码时**不回显**（安全机制），并非没输入进去。
- 创建成功后会在 `/home` 下生成对应用户目录，并分配 **UID（用户 ID）**、**GID（组 ID）**。
- 密码过短（如 123456）系统会提示，但学习阶段仍可继续。

**补充——常用指令速查：**

| 指令 | 说明 |
| --- | --- |
| `pwd` | 显示当前路径 |
| `mkdir 目录名` | 新建目录 |
| `rm 文件` | 删除文件 |
| `cp 源 目标` | 复制 |
| `mv 源 目标` | 移动/重命名 |
| `passwd 用户名` | 修改用户密码 |

## 十二、易错点与操作复盘

- 命令与对象之间忘加空格，系统无法识别。
- Linux 大小写敏感，`LS` ≠ `ls`。
- 普通用户直接 `useradd` 会因权限不足失败，需加 `sudo`。
- 输入密码不显示星号是正常现象，不代表没输入。
- 虚拟机资源不要分配过高，避免拖慢宿主机。
- 桥接模式下若网络图标为问号，表示未联网；联网后会变化为已连接图标。

## 十三、复习自测题

1. 写出嵌入式系统的定义及四个关键词。
2. 嵌入式系统由哪三大部分组成？
3. ARM 有哪三种含义？ARM 公司的盈利模式是什么？
4. 比较冯·诺依曼架构与哈佛架构的差异。
5. RISC 与 CISC 分别指什么？ARM 采用哪种？
6. 简述嵌入式系统开发的整体流程。
7. 比较自顶向下与自底向上设计方法的优缺点。
8. 什么是交叉编译？为什么嵌入式开发需要它？
9. VMware 中“创建虚拟机”与“打开虚拟机”的区别？
10. Linux 根目录下 `/bin`、`/etc`、`/home`、`/tmp` 的作用分别是什么？
11. `ls`、`cd /home`、`clear`、`sudo useradd david` 分别完成什么功能？
12. Linux 输入密码时为什么不显示任何字符？