# 嵌入式系统设计原理与应用 · 第 1 课笔记（扩展版）

> 本版在原课堂笔记基础上，结合课程知识与行业常识进行了大幅扩充与解析，新增章节、图表、术语表和自测题参考答案。
> 标记说明：**★ 课堂重点**；**扩充**表示基于已有知识的扩展解析。
> 原始录音转写存在口语化、同音字识别误差，笔记表述以整理后的规范说法为准。

## 一、本课重点速览

| 模块 | 核心内容 | 记忆关键词 |
| --- | --- | --- |
| 嵌入式系统定义 | 专用的计算机系统 | 应用为中心、软硬件可裁剪 |
| 系统组成 | 三大部分 | 硬件 + 嵌入式操作系统 + 嵌入式软件 |
| ARM | 三种含义、卖 IP 核 | 公司 / 微处理器 / 技术 |
| 架构对比 | 冯·诺依曼 vs 哈佛 | 总线合一 vs 总线分离 |
| 开发流程 | 定义需求 → 硬件/软件并行 → 验收闭环 | 产品经理、流程图 |
| 设计方法 | 自顶向下为主、自底向上为辅 | 模块化、高内聚低耦合 |
| 环境准备 | VMware + Ubuntu 24 | 打开虚拟机、密码 123456 |
| Linux 指令 | ls、cd、clear、useradd | 命令 空格 对象、大小写敏感 |

## 二、嵌入式系统的定义 ★

### 2.1 标准定义

嵌入式系统是**以应用为中心、以计算机技术为基础、软硬件可裁剪**，适用于应用系统对**功能、可靠性、成本、功耗**等有严格要求的**专用计算机系统**。

### 2.2 关键词逐个拆解

1. **以应用为中心**：按具体任务定制，如空调控制器只负责温控，不是通用设备。
2. **以计算机技术为基础**：本质还是“计算机”，具备处理器、存储器、输入输出和信息处理能力。
3. **软硬件可裁剪**：硬件按需减少外设、软件按需裁剪模块，以省成本、降功耗。
4. **专用计算机系统**：与通用 PC 相对，通常只完成特定功能。

### 2.3 嵌入式系统的主要特点（扩充）

| 特点 | 说明 |
| --- | --- |
| 专用性 | 面向特定应用，软硬件按任务定制 |
| 可裁剪性 | 硬件、软件均可按需增删 |
| 实时性 | 多数系统要求在规定时间内响应（如汽车安全气囊毫秒级触发） |
| 资源受限 | 内存、Flash、功耗、体积、成本都受约束 |
| 高可靠性 | 长期运行、环境恶劣（高温、振动、电磁干扰）仍需稳定 |
| 软硬件紧密耦合 | 开发常需软硬件协同考虑，甚至共同设计 |

### 2.4 嵌入式系统 vs 通用计算机（扩充）

| 维度 | 嵌入式系统 | 通用计算机 |
| --- | --- | --- |
| 定位 | 专用功能 | 通用任务 |
| 软硬件 | 可裁剪、资源受限 | 资源丰富、可扩展 |
| 功耗/体积 | 低功耗、小体积 | 功耗和体积较大 |
| 操作系统 | 裁剪定制（Linux、RTOS，甚至裸机） | Windows、macOS 等 |
| 交互方式 | 多为按键、传感器、无显示器 | 键盘鼠标 + 显示器 |
| 开发调试 | 交叉开发、烧录调试 | 本机开发调试 |
| 升级维护 | 通常较难，需要专门工具 | 方便 |

### 2.5 常见考法

- 填空：嵌入式系统是“以___为中心、以___为基础、软硬件___”的“___计算机系统”。
- 简答：简述嵌入式系统的特点。

## 三、嵌入式系统的组成 ★

### 3.1 三大部分（课堂强调）

1. **硬件层**
   - 核心是**处理器（芯片）**，如 ARM 处理器、8051 内核单片机。
   - 外围硬件：内存条/硬盘、USB、VGA/HDMI、投影、传感器等。
2. **嵌入式操作系统**
   - 运行在硬件之上，比 PC 操作系统精简、可裁剪，如嵌入式 Linux、FreeRTOS。
3. **嵌入式软件**
   - 面向具体应用的程序，最终下载/烧录到目标硬件上运行。

### 3.2 更完整的四层结构（扩充）

| 层次 | 内容 | 作用 |
| --- | --- | --- |
| 应用层 | 应用程序 | 实现具体业务功能 |
| 操作系统层 | 内核、文件系统、网络协议栈 | 管理硬件资源、提供系统服务 |
| 中间层 | BSP / 硬件抽象层 HAL / 设备驱动 | 屏蔽硬件差异，向上提供统一接口 |
| 硬件层 | CPU、存储、I/O、外设、总线 | 系统的物理基础 |

**理解要点：** 课堂上的“三部分”是**宏观分类**；教材中常见的“中间层”本质是软硬件之间的适配层，属于硬件抽象，不必与“三部分”对立记忆。

### 3.3 硬件核心部件（扩充）

- **CPU/处理器**：执行指令。分为 MCU（单片机）、MPU（微处理器）、DSP、SoC 等（见第十四章）。
- **存储器**：RAM（运行程序）、ROM/Flash（存放固件和掉电不丢的数据）。
- **I/O 与总线**：GPIO、UART、SPI、I2C、CAN、USB、以太网等连接外设。
- **电源与时钟**：供电电路、晶振，保证稳定运行。

## 四、ARM 与嵌入式微处理器 ★

### 4.1 两大主流架构

| 架构 | 典型设备 | 指令集 | 特点 |
| --- | --- | --- | --- |
| x86 | PC、笔记本、服务器 | CISC（复杂指令集） | 性能强、功耗高、生态成熟 |
| ARM | 手机、平板、嵌入式设备 | RISC（精简指令集） | 低功耗、低成本、广泛应用 |

**扩充——为什么手机不用 x86？** 手机对功耗和散热极敏感，ARM 以低功耗设计见长；x86 高性能对应高功耗，适合需要插电、散热的场景。近年来 ARM 也开始进入笔记本/服务器（如 Apple M 系列芯片）。

### 4.2 ARM 的三种含义 ★

1. **公司名称**：ARM 公司（Advanced RISC Machines）。成立于 1990 年，前身与 Acorn 公司有关。
2. **微处理器的统称**：基于 ARM 内核设计、生产的处理器。
3. **技术名称**：ARM 架构本身（指令集、内核设计规范）。

### 4.3 ARM 的商业模式：卖 IP 核 ★

- ARM 公司**自己不直接制造芯片**，主要**研发并授权 IP 核（知识产权核，Intellectual Property core）**。
- IP 核是“芯片的架构与核心设计”，类似一套“图纸/设计方案”，**不是网络 IP 地址**。
- 授权后，芯片设计公司付费获得使用权（许可证费 + 版税）。

**扩充——IP 核的三种形态：**

| 类型 | 英文 | 特点 | 灵活性 |
| --- | --- | --- | --- |
| 软核 | Soft Core | 以 HDL 代码（Verilog/VHDL）提供，可综合重配置 | 高 |
| 固核 | Firm Core | 综合后的网表/布局约束，跨工艺需重优化 | 中 |
| 硬核 | Hard Core | 已定版图、工艺固定，性能确定、不可修改 | 低 |

### 4.4 芯片产业链（扩充）

```
IP 授权（ARM）
   ↓
芯片设计（高通 Qualcomm、三星、联发科等，获得授权后设计完整芯片）
   ↓
晶圆代工（台积电 TSMC、中芯国际 SMIC、长江存储等）
   ↓
渠道/封装测试 → 零售商 → 终端用户（淘宝/京东买到的芯片）
```

- 平时从网上买到的芯片，源头一定是经过“IP 授权 → 设计 → 代工”层层递进的。
- 华为、小米、三星、OPPO 等手机厂商多使用高通/联发科等授权芯片方案。

### 4.5 8051 单片机背景（扩充，便于理解“内核”）

- 主流单片机（STC、AT89 系列等）的**内核**是 **8051**。
- 51 与 52 的区别主要在**片内 RAM 大小**，引脚定义、40 引脚封装基本一致。
- 4 个 I/O 口：P0、P1、P2、P3，每口 8 位（P0.0 ~ P0.3 等），可用于 LED 控制、按键、中断、串口通信等实验。
- I/O 类型：输入（Input）、输出（Output）、双向（既入又出）。
- 8051 是 **8 位处理器**（寄存器 8 位）；而 STM32 是 **32 位处理器**。
- 8 位单片机的数据手册（寄存器、位定义）是核心资料；32 位处理器寄存器更多、更复杂，直接查手册开发效率低，通常用**库函数/驱动库**开发（如 STM32 标准库、HAL 库）。

### 4.6 防静电知识（扩充）

- 人体静电瞬时放电可达**上万伏**，冬天脱毛衣、摸金属时的“啪”就是静电放电。
- 静电可能击穿芯片内部电路，造成永久损坏。
- 芯片/电路板用**防静电袋**包装；操作时注意防静电手环、接地。

## 五、ARM 系列与架构对比 ★

### 5.1 ARM 系列演进

| 系列 | 架构 | 特点 |
| --- | --- | --- |
| ARM7 | 冯·诺依曼架构 | 成熟、大批量、3 级流水线、RISC、低功耗，主频可达约 130 MIPS |
| ARM9 / ARM10 / ARM11 | 哈佛架构 | 程序与数据总线分离，流水线等级更高（五六七八级），效率更高 |
| Cortex 系列 | 按应用细分 | Cortex-A（应用）、Cortex-R（实时）、Cortex-M（单片机），STM32 采用 Cortex-M |

### 5.2 Cortex 三大系列（扩充）

| 系列 | 全称 | 目标 | 典型应用 |
| --- | --- | --- | --- |
| Cortex-A | Application | 高性能应用处理器 | 手机 SoC、平板、智能电视、树莓派 |
| Cortex-R | Real-time | 硬实时、高可靠 | 汽车刹车/气囊、硬盘控制器、通信基站 |
| Cortex-M | Microcontroller | 低功耗单片机 | STM32、GD32 等 MCU，物联网设备 |

**扩充——Thumb 指令集：** ARM 为节省存储空间设计了 Thumb（16 位）指令，Thumb-2 是 16/32 位混合指令集，在代码密度与性能间取得平衡，Cortex-M 系列广泛使用。

### 5.3 冯·诺依曼架构 ★

- **组成**：运算器、控制器、存储器、输入设备、输出设备；CPU = 运算器 + 控制器。
- **特点**：**程序指令与数据共用同一总线**，取指令和取数据不能同时进行，可能出现“拥堵”，结构简单、成本低。
- **典型**：早期 8051（实际为改进型）、早期计算机。

### 5.4 哈佛架构 ★

- **特点**：**指令存储与数据存储分离，总线相互独立**，可同时取指令和读写数据，效率更高。
- **类比**：教室两个门，一个进一个出，比单门同时进出快。
- **典型**：ARM9 及之后、现代单片机（AVR、PIC、STM32）多为哈佛/改进哈佛结构。

**扩充——流水线（Pipeline）：** 处理器把指令执行分为“取指 → 译码 → 执行（→写回）”等阶段，多级流水线可让多条指令重叠执行，提高吞吐量。ARM7 为 3 级流水线，ARM9 为 5 级，级别越高理论上吞吐越快（但更复杂、更依赖编译优化和分支预测）。

### 5.5 RISC vs CISC（扩充）

| 维度 | RISC（ARM） | CISC（x86） |
| --- | --- | --- |
| 指令 | 少而精、定长、简单 | 多而复杂、变长 |
| 寻址方式 | 少，以寄存器操作为主 | 多、灵活 |
| 执行 | 大部分单周期，适合流水线 | 复杂指令微码实现 |
| 功耗 | 低 | 高 |
| 编译 | 依赖编译器的优化 | 硬件承担更多工作 |

## 六、嵌入式系统开发流程 ★

### 6.1 三大开发

嵌入式系统开发包括：**总体开发、硬件开发、软件开发**。

### 6.2 详细流程（扩充）

```
系统定义与可行性分析（需求调研，产品经理）
        ↓
需求规格说明书（SRS，明确功能/性能/约束）
        ↓
    ┌─── 硬件开发 ───┐      ┌─── 软件开发 ───┐
    │ 原理图设计      │      │ 需求分析        │
    │ PCB 设计        │      │ 概要/详细设计    │
    │ 制板/焊接       │      │ 编码            │
    │ 硬件调试        │      │ 编译/单元测试    │
    └───┬───────────┘      └───┬───────────┘
        └──────── 软硬件联调（验证与测试）────────┘
                    ↓
              验收；不合格则返回修改（闭环大循环）
                    ↓
                  交付与维护
```

- **产品经理**：立项前与甲方对接、做需求调研，把功能、性能、规格“定死”，避免反复改动。
- **验收环节**：软硬件适配跑通才算合格；任何环节不符合需求都要返回修改，形成大循环。
- **流程图是重点**：顺序步骤用流程框（矩形），判断条件用判断框（菱形），对应代码中的 `if`、`if-else`、`switch-case`。

### 6.3 补充：开发模型（扩充）

| 模型 | 特点 | 适用 |
| --- | --- | --- |
| 瀑布模型 | 阶段顺序推进，上一阶段做完整再进入下一阶段 | 需求明确的简单项目 |
| 迭代/增量 | 分块开发、逐步完善 | 需求易变的中大型项目 |
| 螺旋模型 | 需求→设计→原型→风险评估循环 | 高风险项目 |

实际嵌入式项目常**软硬件并行协同设计**：一边画板子，一边写驱动和应用。

## 七、自顶向下 vs 自底向上 ★

### 7.1 对比

| 方法 | 思路 | 类比 | 优缺点 |
| --- | --- | --- | --- |
| 自顶向下 Top-Down | 大系统按模块拆分，先总体后细化 | 装修教室：先分讲台、桌椅、照明、投影模块 | 单模块修改不影响其他模块，易维护 |
| 自底向上 Bottom-Up | 从底层一步步向上搭建 | 盖房子：先打地基再砌墙 | 底层出问题需层层拆除，修改成本高 |

### 7.2 课堂案例复盘

- **装修教室**：分成老师讲台/黑板、学生课桌、照明、投影四部分模块；桌椅摆放不合理只改桌椅模块，不动其他部分。
- **照明**：灯管横向摆放时灯在身后，影子会遮住作业本；纵向摆放阴影更小，更合理。
- **盖房子**：地基不牢（如只打 2 米而非 7 米），上层墙体、房梁都会出问题，返工需层层拆。

### 7.3 扩充：模块化设计原则

- **高内聚（High Cohesion）**：模块内部功能紧密相关。
- **低耦合（Low Coupling）**：模块之间依赖尽量少。
- 好处：便于单独开发、测试、替换、复用，一处修改不易影响全局。

### 7.4 结论

实际设计中两者**交替结合**：先用自顶向下拆分模块，再对重点模块用自底向上方式细化实现，但整体以**自顶向下**为主体。
## 八、交叉编译 ★

### 8.1 为什么要交叉编译（扩充）

- 目标设备（ARM）的资源有限，不能安装完整的开发工具链，通常**不在目标板上开发**。
- 开发机（台式机/笔记本，x86 架构）上编译出的程序**不能直接在 ARM 上运行**（指令集不同、ABI 不同）。
- 解决：在**宿主机（Host）**上用**交叉编译器**，生成在**目标机（Target）**（ARM）上运行的代码。

### 8.2 概念对比

| 编译方式 | 宿主机 | 目标机 | 工具 |
| --- | --- | --- | --- |
| 本机编译 | x86 PC | x86 PC | gcc（本地） |
| 交叉编译 | x86 PC（开发机） | ARM 等嵌入式目标 | 交叉编译器 |

### 8.3 交叉工具链构成（扩充）

| 组件 | 作用 |
| --- | --- |
| binutils | 汇编器 as、链接器 ld、目标文件工具 objcopy/objdump 等 |
| GCC 交叉编译器 | 生成目标架构的汇编/机器码，如 `arm-linux-gnueabihf-gcc` |
| C 运行库 | glibc / musl / uClibc，提供 printf 等函数实现 |
| GDB 调试器 | 远程/模拟调试目标程序 |

**工具链前缀解读（扩充）：** `arm-linux-gnueabihf-gcc` 中 `arm` 指目标架构，`linux` 指目标系统，`gnueabihf` 指使用 GNU 工具链 + hard-float（硬件浮点）。前缀不同，生成的代码适用平台就不同。

### 8.4 典型流程

```
在 PC 上编写源码（.c / .h）
   ↓
交叉编译（arm-linux-gnueabihf-gcc）
   ↓
生成 ARM 可执行文件/目标文件（.elf / .bin / .hex）
   ↓
烧录/下载到目标板（JTAG/SWD、串口、网口 TFTP、U 盘）
   ↓
在目标板上运行、调试
```

**扩充——软件下载方式：**

| 方式 | 说明 |
| --- | --- |
| 调试器烧录 | J-Link / ST-Link，通过 JTAG/SWD 下载调试 |
| 串口 ISP | 通过 UART 引导程序下载（如 STC、Bootloader） |
| 网络 TFTP/NFS | 开发板上通过网口取文件，方便反复调试 |
| 存储介质 | SD 卡、U 盘拷贝 |

### 8.5 补充：构建系统（扩充）

项目较大时用 **Makefile** 或 **CMake** 组织编译：只需一条 `make` 命令即可按依赖关系编译整个工程，避免手动敲多条 gcc 命令；结合交叉编译器指定工具链，可一键生成目标板固件。

## 九、环境准备：VMware + Ubuntu ★

### 9.1 文件与安装

- 提供的文件夹通常包含：**Ubuntu 24 完整系统（非 ISO 镜像）**、**VMware 安装程序**、**激活码**。
- VMware 安装：常规“下一步”，最后一步输入激活码即可完成。

### 9.2 VMware 三个主要选项

1. **创建虚拟机**：从 ISO 镜像文件全新安装一个操作系统（如 Windows、Ubuntu）。
2. **打开虚拟机**：直接打开别人做好的完整系统（本课程使用，推荐）。
3. **远程连接服务器**：连接远程虚拟化服务器，本课程暂不涉及。

### 9.3 资源分配建议（扩充）

| 配置项 | 课程示例 | 建议 |
| --- | --- | --- |
| 内存 | 4 GB | 根据宿主机内存分配，别超过一半，避免卡顿 |
| 处理器 | 2 核 | 1~4 核，够用即可 |
| 硬盘 | 20 GB | 系统 + 工具链，20~40 GB 较舒适 |

原则：虚拟机**占用宿主机真实资源**，分配过多会导致宿主机和虚拟机都卡。

### 9.4 关机 / 挂起 / 重启

| 操作 | 含义 | 类比 |
| --- | --- | --- |
| 关机（关机） | 关闭虚拟机操作系统 | 电脑关机 |
| 挂起（挂起） | 锁定当前状态，运行现场保留 | “冷冻/休眠”，下次直接打开继续 |
| 重启（重置客户端） | 重启虚拟机 | 电脑重启 |

**扩充——快照（Snapshot）：** VMware 支持给虚拟机拍“快照”，记录某个时刻的系统状态；出问题可以随时恢复到快照，是实验课的“后悔药”。

### 9.5 Ubuntu 登录

- 账户名见老师提供；密码：**123456**（学习阶段使用，正式环境密码要复杂）。

### 9.6 三种网络模式（扩充）

| 模式 | 原理 | 虚拟机能否上网 | 能否与宿主机互访 | 能否被局域网其他机器访问 | 典型场景 |
| --- | --- | --- | --- | --- | --- |
| 桥接 Bridged | 虚拟机直接占用局域网一个地址，如同真实主机 | 能（需路由器 DHCP） | 能 | 能 | 需要对外提供服务、模拟真实网络设备 |
| NAT | 宿主机做“网关”，虚拟机共享宿主机 IP 上网 | 能 | 能 | 不能 | 最常见，仅需联网学习 |
| 仅主机 Host-Only | 虚拟机与宿主机组成私有网络 | 不能上网 | 能 | 不能 | 虚拟机间通信、隔离实验 |

**课堂细节：** 桥接模式下网络图标是问号表示未联网；连上手机热点成功后图标会变为已连接样式。

## 十、Linux/Ubuntu 基本认知 ★

### 10.1 一切皆文件

- 普通文件、文件夹、设备（鼠标、键盘、硬盘）、网络接口在 Linux 中都抽象为“文件”。
- 例如设备在 `/dev` 下体现，配置在 `/etc` 下体现。
- 好处：统一了操作接口，很多操作可以像操作文件一样完成。

### 10.2 目录结构（扩充：FHS 常见目录）

| 目录 | 作用 | 说明 |
| --- | --- | --- |
| `/` | 根目录 | 所有文件的起点 |
| `/bin` | 系统基本命令（二进制） | ls、cp 等常驻 |
| `/sbin` | 管理员命令 | 一般需 root |
| `/boot` | 启动文件 | 内核、引导加载器 |
| `/dev` | 设备文件 | 一切设备的“文件”表现 |
| `/etc` | 配置文件 | 系统网络、服务、软件配置 |
| `/home` | 用户主目录 | 每个用户一个文件夹，日常主要在这里 |
| `/root` | root 用户主目录 | 管理员的家 |
| `/lib` | 共享库 | 程序运行依赖的 .so |
| `/mnt`、`/media` | 挂载目录 | U 盘/光盘等临时挂载 |
| `/tmp` | 临时文件 | 重启常清空 |
| `/usr` | 用户软件资源 | 程序、库、头文件 |
| `/var` | 可变数据 | 日志、缓存、邮件等 |

- Windows 用盘符（C、D、E…）；Linux 是**单一根目录 `/` + 目录树**。
- 常见二级目录：`/bin`、`/boot`、`/dev`、`/etc`、`/home`、`/mnt`、`/tmp` 等。

### 10.3 挂载（Mount，扩充）

- 把存储设备（分区、U 盘）接入目录树的过程叫**挂载**。
- 如 U 盘插入后出现在 `/media/用户名/U盘名`。
- 相关命令：`mount`、`umount`；文件系统类型常见 ext4、vfat（FAT32）、NTFS、exFAT。

### 10.4 用户与权限（扩充）

- Linux 是多用户系统，用 **UID（用户 ID）** 和 **GID（组 ID）** 区分用户/组。
- 每个文件有三组权限：**所有者（user）、所属组（group）、其他用户（others）**，各含 **r（读）/ w（写）/ x（执行）**。
- 普通用户默认不能修改系统目录，日常操作放在 `/home`。
- **root（超级用户）** 拥有最高权限；`sudo` 可临时以管理员权限执行命令。

### 10.5 终端操作

- 右键“在终端打开”，绝大多数操作在终端完成。
- 放大字体：`Ctrl+Shift+=`；缩小：`Ctrl+-`。
- 实验截图建议把终端背景调成**白色**，避免打印时“漆黑一片”。

## 十一、Linux 指令基础 ★

### 11.1 基本规则

- 指令格式：`命令 [选项] 操作对象`，命令与对象之间**必须有空格**。
- **大小写敏感**：`ls` 有效，`LS` 报“未找到命令”。
- **Shell**：既是**命令解释器**，也是**脚本编程语言**；脚本后缀常为 `.sh`。

### 11.2 命令分类（扩充）

文件管理、用户管理、打包压缩、磁盘管理、网络管理、进程管理、系统状态、软件包管理等。

### 11.3 课堂演示命令详解

| 指令 | 作用 | 常用选项/补充 |
| --- | --- | --- |
| `ls` | 列出当前目录内容 | `ls -l`（详细信息）、`ls -a`（含隐藏文件） |
| `cd /home` | 切换目录 | `cd ..` 返回上级，`cd ~` 回主目录 |
| `clear` | 清屏 | 快捷键 `Ctrl+L` |
| `sudo useradd 用户名` | 添加用户 | 需要管理员权限 |

### 11.4 useradd 实操要点 ★

- 添加用户需要 **root 权限**：先 `sudo` 提权，或先 `su -` 切换到 root。
- `useradd` 示例：`sudo useradd david`，成功后系统分配 UID/GID，并在 `/home` 生成 `david` 目录。
- 密码设置用 `passwd david`；输入密码**不回显**（安全机制），不是没输入。
- 密码过短（如 123456）系统有提示，学习阶段可继续。

### 11.5 扩充：常用指令速查表

| 分类 | 指令 | 说明 |
| --- | --- | --- |
| 文件 | `pwd` | 显示当前路径 |
| 文件 | `touch 文件` | 新建空文件 |
| 文件 | `mkdir 目录` | 新建目录（`-p` 可建多级） |
| 文件 | `cat 文件` | 查看文件内容 |
| 文件 | `cp 源 目标` | 复制 |
| 文件 | `mv 源 目标` | 移动/重命名 |
| 文件 | `rm 文件` | 删除（`-r` 递归删除目录，慎用） |
| 权限 | `chmod 755 文件` | 修改权限（r=4,w=2,x=1） |
| 权限 | `chown 用户 文件` | 修改所有者 |
| 用户 | `passwd 用户名` | 设置/修改密码 |
| 用户 | `su -` / `sudo 命令` | 切换用户 / 提权执行 |
| 网络 | `ip addr` / `ifconfig` | 查看 IP |
| 网络 | `ping 地址` | 测试连通性 |
| 进程 | `ps -ef` / `top` | 查看进程 / 实时状态 |
| 磁盘 | `df -h` / `du -sh 目录` | 磁盘/目录占用 |
| 查找 | `grep 关键词 文件` | 文本搜索 |
| 查找 | `find 路径 -name 名称` | 按名字找文件 |
| 压缩 | `tar -czvf x.tar.gz 目录` | 打包压缩 |
| 解压 | `tar -xzvf x.tar.gz` | 解压 |
| 软件 | `sudo apt update && sudo apt install 包` | Ubuntu 安装软件 |
| 帮助 | `man 命令` / `命令 --help` | 手册/帮助 |

**扩充——管道与重定向：**

| 符号 | 作用 | 示例 |
| --- | --- | --- |
| `\|` | 管道：前一个命令输出作为后一个命令输入 | `ls -l \| grep txt` |
| `>` | 输出重定向（覆盖） | `echo hi > a.txt` |
| `>>` | 输出重定向（追加） | `echo hi >> a.txt` |

## 十二、易错点与操作复盘 ★

1. 命令与对象之间**忘加空格**，系统无法识别。
2. Linux **大小写敏感**：`LS` ≠ `ls`、`Cd` ≠ `cd`。
3. 普通用户直接 `useradd` 提示权限不足，要加 `sudo`。
4. 输入密码**不显示任何字符**是正常现象，不代表没输入。
5. 虚拟机资源分配过高会拖慢宿主机。
6. 桥接模式网络图标为问号 = 未联网。
7. `sudo` 与 `su` 的区别：`sudo 命令`是单条命令临时代理，`su -` 是切换成 root 用户。
8. `rm -rf` 十分危险，删除前务必确认路径。
9. 修改系统配置（`/etc` 下）前先备份，权限不对会导致系统无法启动。
10. Windows 习惯的“双击盘符”在 Linux 不存在，要按路径一层层访问。

## 十三、复习自测题（含参考答案）

### 基础题（课堂知识点）

1. 写出嵌入式系统的定义及四个关键词。
2. 嵌入式系统由哪三大部分组成？
3. ARM 有哪三种含义？ARM 公司的盈利模式是什么？
4. 比较冯·诺依曼架构与哈佛架构的差异。
5. RISC 与 CISC 分别指什么？ARM 采用哪种？
6. 简述嵌入式系统开发的整体流程。
7. 比较自顶向下与自底向上设计方法的优缺点。
8. 什么是交叉编译？为什么嵌入式开发需要它？
9. VMware 中“创建虚拟机”与“打开虚拟机”的区别？
10. Linux 根目录下 `/bin`、`/etc`、`/home`、`/tmp` 的作用分别是什么？
11. `ls`、`cd /home`、`clear`、`sudo useradd david` 分别完成什么功能？
12. Linux 输入密码时为什么不显示任何字符？

### 扩展题（扩充知识）

13. MCU、MPU、SoC 的区别？
14. Cortex-A、Cortex-R、Cortex-M 分别面向什么场景？
15. 裸机、RTOS、嵌入式 Linux 分别适合什么场合？
16. 交叉工具链 `arm-linux-gnueabihf-gcc` 各部分含义是什么？
17. 桥接、NAT、仅主机三种网络模式的主要区别？
18. Linux 权限 `rwx` 与数字 `755` 如何对应？
19. 什么是“一切皆文件”，举例说明。
20. 软核、固核、硬核的区别？

### 参考答案

1. 以应用为中心、以计算机技术为基础、软硬件可裁剪，适用对功能、可靠性、成本、功耗有严格要求的专用计算机系统；关键词：应用为中心、计算机技术为基础、软硬件可裁剪、专用。
2. 硬件层、嵌入式操作系统、嵌入式软件。
3. 公司名称、微处理器统称、技术名称；ARM 不直接造芯片，靠授权 IP 核收取许可费与版税。
4. 冯·诺依曼：程序与数据共用一条总线，结构简单；哈佛：程序与数据总线分离，可并行访问，效率更高。
5. RISC 精简指令集，CISC 复杂指令集；ARM 采用 RISC。
6. 系统定义与需求分析 → 硬件/软件开发并行 → 软硬件联调 → 验收（不合格返回修改）→ 交付维护。
7. 自顶向下：模块化拆分、易维护、单点修改影响小；自底向上：逐层搭建，底层出问题返工成本高；实际以自顶向下为主、二者结合。
8. 在宿主机（x86）上用交叉编译器生成目标机（ARM）可执行代码；因为目标机资源少、且 x86 编译的程序不能在 ARM 上运行。
9. 创建虚拟机是从 ISO 镜像全新安装系统；打开虚拟机是直接使用现成的完整系统。
10. `/bin` 基本命令、`/etc` 配置文件、`/home` 用户主目录、`/tmp` 临时文件。
11. `ls` 列出目录内容；`cd /home` 进入 /home；`clear` 清屏；`sudo useradd david` 添加用户 david。
12. 安全机制：防止他人偷看密码；字符已输入只是不显示。
13. MCU 单片机（CPU+存储+外设一体的芯片）；MPU 微处理器（只含 CPU，需外接存储/外设）；SoC 系统级芯片（CPU+DSP/GPU+通信等集成，如手机芯片）。
14. A 高性能应用；R 硬实时高可靠；M 低功耗单片机。
15. 裸机适合简单控制、无复杂并发；RTOS 适合多任务、有一定实时性要求；嵌入式 Linux 适合功能复杂、资源充足（需要网络/文件系统/多媒体）。
16. arm=目标架构，linux=目标系统，gnueabihf=GNU 工具链 + 硬件浮点，gcc=C 编译器。
17. 桥接像真实主机可互访能上网；NAT 共享宿主机上网、宿主机可访问虚拟机，外部不可直连；仅主机只能与宿主机通信，不能上网。
18. r=4、w=2、x=1；755 = 所有者 rwx(7)、组 r-x(5)、其他人 r-x(5)。
19. 普通文件、文件夹、设备、网络接口等都以“文件”形式抽象，可通过统一接口读写。
20. 软核以代码形式提供、灵活可重配置；固核为网表/布局形式；硬核为定版图形式、性能确定不可改。

## 十四、嵌入式系统分类与处理器选型（扩充）

### 14.1 常见处理器分类

| 类型 | 英文 | 组成特点 | 典型 | 应用 |
| --- | --- | --- | --- | --- |
| 单片机 | MCU | CPU+存储+外设集成 | 8051、AVR、STM32 | 家电、工业控制、传感 |
| 微处理器 | MPU | 仅 CPU，需外接存储与外设 | ARM Cortex-A、x86 | 需要大内存的嵌入式 Linux |
| 数字信号处理器 | DSP | 专长大量乘加运算 | TMS320、SHARC | 音频、图像、雷达信号处理 |
| 系统级芯片 | SoC | 多核/多模块集成 | 骁龙、麒麟、苹果 A 系 | 手机、平板、AI 边缘 |
| FPGA | 可编程逻辑器件 | 硬件电路可编程 | Xilinx、Intel/Altera | 高速、并行、原型验证 |

**扩充——MCU 与 MPU 区分：** MCU 把 CPU、RAM、Flash、外设集成在一个芯片，便宜、功耗低、适合控制；MPU 只有 CPU，需要外接 DDR、存储等，性能强但成本和功耗高，常跑 Linux。

### 14.2 处理器选型因素（扩充）

1. 性能：主频、MIPS/DMIPS、浮点能力。
2. 功耗与散热：电池供电选低功耗 MCU。
3. 外设：需要哪些接口（UART/SPI/I2C/CAN/USB/以太网）。
4. 存储：片内 Flash/RAM 是否够用。
5. 开发环境与生态：工具链、库、例程、社区。
6. 成本与供货：BOM 成本、长期供货、国产替代。

## 十五、嵌入式操作系统与实时性（扩充）

### 15.1 三类软件形态

| 形态 | 说明 | 适用 |
| --- | --- | --- |
| 裸机（前后台） | 主循环 + 中断，无操作系统 | 简单任务、成本极低 |
| RTOS（实时操作系统） | 多任务调度，有优先级、信号量等 | 多任务、实时性要求较高 |
| 嵌入式 Linux | 功能完整，含网络、文件系统、GUI | 功能复杂、资源充足 |

### 15.2 实时性概念

- **硬实时**：必须在截止时间内完成，否则算系统失效（如安全气囊、刹车）。
- **软实时**：偶尔超时影响体验但不致命（如视频播放卡顿）。
- RTOS 通过**任务优先级调度 + 时间片**保证关键任务及时执行。

### 15.3 RTOS 核心机制（扩充）

- 任务（Task/线程）：独立执行单元，有优先级。
- 调度：可抢占式调度，高优先级先运行。
- 同步互斥：信号量（Semaphore）、互斥锁（Mutex）保护共享资源。
- 通信：消息队列（Queue）、事件、邮箱。
- 中断：中断服务程序（ISR）快速响应硬件事件。

### 15.4 常见嵌入式操作系统（扩充）

| 系统 | 特点 | 典型场景 |
| --- | --- | --- |
| FreeRTOS | 开源免费、小巧 | STM32、ESP32，物联网设备 |
| μC/OS | 经典教学 RTOS | 教学、工控 |
| RT-Thread | 国产开源物联网 OS | 国产 MCU、IoT |
| VxWorks | 商业、高可靠硬实时 | 航空航天、军工、汽车 |
| QNX | 微内核、汽车级安全 | 车载系统 |
| 嵌入式 Linux | 开源、生态大 | 路由器、工业平板、边缘网关 |
| HarmonyOS（鸿蒙） | 分布式操作系统 | 全场景物联网 |

## 十六、嵌入式软件开发与调试（扩充）

### 16.1 开发四步

```
编写源码（编辑）→ 交叉编译（编译+链接）→ 下载烧录 → 在线调试
```

- **编译**：源文件 → 目标文件（.o）。
- **链接**：目标文件 + 库 → 可执行文件/固件（.elf 等），链接脚本决定代码、数据、堆栈地址布局。
- **启动文件**：芯片上电后先执行启动代码（向量表、时钟初始化、搬移数据），再进入 main。

### 16.2 调试手段

| 手段 | 说明 |
| --- | --- |
| 打印输出 | printf 通过串口输出到电脑，最简单 |
| 调试器 | GDB + JTAG/SWD（J-Link、ST-Link），可打断点、单步、看变量 |
| 示波器/逻辑分析仪 | 观察时序、电平信号 |
| 在线仿真 | Keil/IAR/IDE 中软件仿真 |

### 16.3 字节序（扩充）

- 大端（Big-Endian）：高位字节在低地址。
- 小端（Little-Endian）：低位字节在低地址，**ARM 默认为小端**，网络协议多用大端（网络字节序）。
- 跨设备通信（串口/CAN/网络）时要注意字节序转换。

## 十七、嵌入式应用领域与学习建议（扩充）

### 17.1 应用领域

- 消费电子：手机、平板、智能手表、MP3、机顶盒。
- 汽车电子：ECU、车载娱乐、ADAS、车联网。
- 工业控制：PLC、电机驱动、工业机器人。
- 医疗电子：监护仪、便携设备、手术机器人。
- 航空航天与军工：飞控、导航、舰载、雷达。
- 物联网：传感器节点、智能家居、边缘网关。
- 网络通信：路由器、交换机、基站。

### 17.2 物联网三层架构（扩充）

| 层次 | 内容 | 嵌入式角色 |
| --- | --- | --- |
| 感知层 | 传感器、摄像头、终端 MCU | 数据采集、边缘处理 |
| 网络层 | 5G/Wi-Fi/LoRa/NB-IoT | 通信模块 |
| 应用层 | 云平台、App | 数据汇聚与业务 |

### 17.3 学习路线建议（扩充）

1. **C 语言与数据结构**：嵌入式开发语言基础。
2. **单片机/ARM**：8051 → STM32（HAL/标准库），掌握 GPIO、定时器、中断、串口、I2C/SPI。
3. **嵌入式 Linux**：环境搭建、交叉编译、驱动（字符设备）、应用与进程线程。
4. **RTOS**：FreeRTOS 任务、信号量、队列。
5. **硬件知识**：数字电路、基础原理图/PCB 阅读、示波器使用。
6. **工具链与工程化**：Makefile/CMake、Git、调试器。

### 17.4 就业方向（扩充）

嵌入式软件工程师、嵌入式驱动工程师、单片机开发、Linux 应用/BSP 开发、物联网开发、FPGA 工程师等。

## 附录 A：术语表

| 术语 | 英文 | 解释 |
| --- | --- | --- |
| ARM | Advanced RISC Machines | 公司名 / 处理器架构统称 |
| IP 核 | Intellectual Property core | 芯片设计知识产权方案 |
| MCU | Microcontroller Unit | 单片机，集成 CPU+存储+外设 |
| MPU | Microprocessor Unit | 微处理器，仅 CPU |
| SoC | System on Chip | 系统级芯片 |
| DSP | Digital Signal Processor | 数字信号处理器 |
| RISC | Reduced Instruction Set Computer | 精简指令集 |
| CISC | Complex Instruction Set Computer | 复杂指令集 |
| BSP | Board Support Package | 板级支持包（硬件抽象） |
| HAL | Hardware Abstraction Layer | 硬件抽象层 |
| RTOS | Real-Time Operating System | 实时操作系统 |
| ISR | Interrupt Service Routine | 中断服务程序 |
| Host / Target | 宿主机 / 目标机 | 开发机 / 运行机 |
| Shell | 外壳 | 命令解释器与脚本语言 |
| Root | 根目录 / 超级用户 | `/` 目录或 root 账户 |
| Mount | 挂载 | 将存储设备接入目录树 |
| UID / GID | User/Group ID | 用户/组标识 |
| JTAG / SWD | 调试接口 | 芯片下载调试接口 |
| ISP / IAP | 系统内编程 / 应用内编程 | 在线烧录方式 |
| Pipeline | 流水线 | 指令重叠执行的硬件机制 |
| Thumb | ARM 16 位指令集 | 提高代码密度 |

## 附录 B：推荐学习资源（扩充）

- 教材：《嵌入式系统设计原理与应用》（课程指定教材）。
- 官方：ARM 官网（arm.com）、STMicroelectronics（STM32）+ STM32CubeMX/HAL、Raspberry Pi 文档。
- Linux：Ubuntu 手册、Man Pages（`man` 命令）、The Linux Documentation Project。
- 社区：电子发烧友、CSDN、GitHub、B站嵌入式教程（韦东山、正点原子等）。
- 工具：Keil MDK、STM32CubeIDE、VS Code + 交叉编译、VMware、J-Link/ST-Link。