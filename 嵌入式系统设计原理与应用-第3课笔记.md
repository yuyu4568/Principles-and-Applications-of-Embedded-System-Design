# 嵌入式系统设计原理与应用 · 第 3 课笔记：Linux 文件操作进阶（mv / rm / 权限 / 链接 / 网络）

> 承接第 2 课（ls、cd、touch、mkdir、cp、pwd）。本课系统讲解 **mv 移动、rm 删除、文件类型识别、权限 rwx 与 chmod、软硬链接 ln**，并简介网络命令。
> 说明：本课笔记在课堂内容基础上做了**更详细的解析**：每个命令都给出含义、格式、参数、示例、对比与易错点；★ 为课堂重点。

## 一、本课重点速览

| 模块 | 命令 | 一句话要点 |
| --- | --- | --- |
| 移动 | `mv` | 移文件和移目录**都不用加 -r**；保留原时间戳；源文件消失 |
| 删除 | `rm` | 删文件不用 -r；删目录必须 `-r`；`-f` 强制 |
| 文件类型 | `ll` 首字符 | `-` 普通文件、`d` 目录、`l` 链接，还有 c/b/p |
| 权限 | `r w x` | 三组 u/g/o，数字 r=4、w=2、x=1 |
| 改权限 | `chmod` | 字符方式（u+x）与数字方式（761） |
| 链接 | `ln` / `ln -s` | 硬链接（同名文件入口）vs 软链接（快捷方式） |
| 网络 | `ifconfig` / `ping` | 查看网卡信息、测试网络连通 |

## 二、课前复习（第 2 课内容回顾）

1. **cp 复制文件 vs 文件夹的区别（课堂提问）**
   - 复制**文件**：`cp 文件 目标目录`，**不加** `-r`；
   - 复制**文件夹**：`cp -r 文件夹 目标目录`，**必须加** `-r`（recursive 递归），否则报错“未指定 -r / omitting directory”。
   - 原因：文件是单一对象；文件夹内部可以包含很多文件和子目录，必须递归复制。
2. **绝对路径与相对路径**
   - 在同一个上级目录下的移动/复制，各目标只是二级目录不同，属于“在本目录内”操作；
   - 跨到并列的其他目录（跨出上级），路径发生“绝对性变化”，要写完整路径（绝对路径）。
   - 类比：从主楼到旁边的楼，和在新楼内部换房间，路径的差别完全不同。

## 三、mv：移动文件或目录 ★

### 3.1 含义

- `mv` = move（移动），作用相当于把东西从一个地方搬到另一个地方。
- 类比：教室从西区 0101 整体搬到 ES0304，原来教室不再有人。

### 3.2 格式与示例

```
mv 源 目标
```

| 示例 | 作用 |
| --- | --- |
| `mv file1 /home/DIR2/` | 把 file1 移动到 /home/DIR2 |
| `mv /home/DIR1/dir1 /home/DIR2/` | 把目录 dir1 移动到 /home/DIR2 |
| `mv old.c new.c` | 改名（移动 + 重命名） |

### 3.3 课堂验证要点 ★

1. **移动目录不需要加 `-r`**：这是 mv 与 cp 的**最大区别**。`mv 目录 目标` 直接可用，无需任何参数。
2. **保留时间戳**：mv 之后目标文件的时间戳**仍是原文件创建的时间**；而 cp 复制后新文件时间戳是**复制时刻**。验证用 `ll`。
3. **源消失**：移动后原路径下文件/目录不再存在；用 `ls` 查看源目录确认“没有了”，再到目标目录 `ls` 确认“到了”。
4. **移动后修改**：把 `mv /home/xxx/DIR1` 改成 `mv /home/xxx/DIR2`，只改目标即可。
5. 查看其他目录不需要进入：比如直接 `ls /home/DIR2` 就能看目标目录内容。

### 3.4 cp vs mv 对比 ★

| 对比项 | cp（复制） | mv（移动） |
| --- | --- | --- |
| 结果 | 原文件保留，多一份副本 | 原文件消失，只留一份 |
| 目录是否需要 -r | **需要** `cp -r` | **不需要**，直接 `mv` |
| 新文件时间戳 | **复制时刻** | **原创建时间（保留）** |
| 类比 | Ctrl+C / Ctrl+V | 剪切 + 粘贴 |

## 四、rm：删除文件或目录 ★

### 4.1 含义

- `rm` = remove（移除、删除）。

### 4.2 格式与参数

```
rm [选项] 目标
```

| 参数 | 作用 |
| --- | --- |
| `-r` | recursive，递归删除目录及其内容；写保护的文件会再确认 |
| `-f` | force，强制删除，不提示 |
| `-rf` | 递归 + 强制（**非常危险，慎用**） |

### 4.3 课堂演示要点

1. **删文件**：`rm file1`。
   - 若文件“有写保护”，系统会提示：`是否删除有写保护的一个普通文件?`，输入 `y` 确认；
   - 若权限不够提示无法删除，则加 `sudo rm file1`。
2. **删目录**：`rm dir2` 会失败，因为目录里可能有文件；需要 `rm -r dir2` 递归删除。
   - 删除后目录不存在（“已删掉”）；可用 `ls` 验证，如删除后目录下只剩一个文件/为空。
3. **权限思路**：删除操作也涉及权限，权限不足用 `sudo` 提权。

### 4.4 文件/目录操作参数总结 ★

| 命令 | 文件 | 目录 |
| --- | --- | --- |
| `cp` | 不加参数 | **加 -r** |
| `mv` | 不加参数 | **不加参数** |
| `rm` | 不加参数 | **加 -r** |

> 记忆：**只有 cp 和 rm 在操作目录时加 -r，mv 一律不加。**

## 五、文件类型：ll 的“第一列” ★

### 5.1 为什么要学

用 `ll`（= `ls -l`）查看文件详细信息时，**第一列第一个字符**表示文件类型，后面 9 个字符表示权限。看懂第一列是读懂 `ll` 的基础。

### 5.2 常见文件类型

| 首字符 | 类型 | 英文 | 说明 |
| --- | --- | --- | --- |
| `-` | 普通文件 | regular file | 最常见：文本、二进制、脚本、C 源码等 |
| `d` | 目录文件 | directory | 即文件夹 |
| `l` | 链接文件 | link | 软链接（快捷方式） |
| `c` | 字符设备 | character device | 如键盘、鼠标、串口 |
| `b` | 块设备 | block device | 如硬盘、U 盘 |
| `p` | 管道文件 | pipe | 进程间通信 |
| `s` | 套接字 | socket | 网络通信 |

### 5.3 与“一切皆文件”的联系

Linux 中普通文件、文件夹、设备、管道、链接……全部以“文件”形式存在，所以叫**一切皆文件**。教材 5.15 节（约 135 页）列出多种文件类型。

### 5.4 示例解读

```
-rw-r--r--  1 asus asus  123 9月20日 11:39 hello.c
d rwxr-xr-x  2 asus asus 4096 9月20日 11:29 dir1
l rwxrwxrwx  1 asus asus   10 9月20日 11:49 shello.c -> hello.c
```

- 第一行：`-` 普通文件，权限 `rw-r--r--`；
- 第二行：`d` 目录文件；
- 第三行：`l` 链接文件（末尾有 `-> hello.c` 指向原文件）。

## 六、权限 rwx 详解 ★

### 6.1 三个符号

| 符号 | 英文 | 含义 |
| --- | --- | --- |
| `r` | read | 可读：能查看/浏览内容 |
| `w` | write | 可写：能编辑并保存 |
| `x` | execute | 可执行：能作为程序运行 |

- 读 ≠ 写：能看（读）不代表能改（写）；写了才能修改内容。
- 对目录而言：r=能列出内容，w=能增删文件，x=能进入目录。

### 6.2 三组对象 u / g / o

| 组 | 英文 | 含义 |
| --- | --- | --- |
| u | user | 文件**所有者**（owner） |
| g | group | **所属组**的用户 |
| o | others | **其他**用户 |

权限共 **9 位**：`u(rwx) + g(rwx) + o(rwx)`，顺序固定不可调换。

### 6.3 数字表示法（8 的 8421 码）★

- 二进制转十进制：`rwx` 每位对应 `4、2、1`（即 2²、2¹、2⁰）。
- 有权限写 1，无权限写 0，然后求和：

| 权限 | 二进制 | 十进制 | 含义 |
| --- | --- | --- | --- |
| `rwx` | 111 | 7 | 读+写+执行 |
| `rw-` | 110 | 6 | 读+写 |
| `r-x` | 101 | 5 | 读+执行 |
| `r--` | 100 | 4 | 只读 |
| `---` | 000 | 0 | 无权限 |

- 示例：`rw-r--r--` = u(6) + g(4) + o(4) = **644**；
- `rwxr-xr-x` = u(7) + g(5) + o(5) = **755**；
- `rwxrwxrwx` = **777**（所有用户最大权限）。

> 补充：这里的“8421”来自 BCD 码/8421 码的概念，即二进制位权 8、4、2、1；本课只需掌握 r=4、w=2、x=1 的换算即可。

## 七、chmod：修改权限 ★

### 7.1 基本要求

- 修改权限需要**管理员权限**：命令前加 `sudo`。
- 语法：`sudo chmod [方式] 文件`

### 7.2 方式一：字符方式（符号模式）

```
chmod [u/g/o/a][+/-/=][r/w/x] 文件
```

| 写法 | 含义 |
| --- | --- |
| `u+w` | 给所有者添加写权限 |
| `g-w` | 去掉所属组写权限 |
| `o+x` | 给其他用户添加执行权限 |
| `a=rwx` | 所有用户（all）设为 rwx |
| `u+x,o-x` | 同时多个操作（逗号分隔） |

课堂演示：

- 大 A 目录原本权限 `rwx r-x r-x`（即 755）；
- `sudo chmod g+w A` → 给 g 组加写，权限变 `rwx rwx r-x`；
- `sudo chmod u-w A` → 去掉 u 组写，权限变 `r-x rwx r-x`；
- `sudo chmod 777 A` → 三组全部 rwx。

### 7.3 方式二：数字方式（绝对模式）

```
sudo chmod 数字组合 文件
```

每一位数字依次表示 u、g、o 的权限值，例如 `chmod 644`：u=rw-、g=r--、o=r--。

**教材例题（约 137/23 页）：** 将 `hello.c` 权限设置为
- 文件所有者：可读可写可执行 → rwx = 7
- 同组用户：可读可写 → rw- = 6
- 其他用户：可执行 → --x = 1

即 `sudo chmod 761 hello.c`。

### 7.4 补充：为什么脚本文件要改权限

- 普通文件新建后默认即可读写；
- **脚本文件**（.sh 等）创建后需要加执行权限才能运行：`sudo chmod +x 脚本.sh`（或 `chmod 755 脚本.sh`）。

## 八、ln：链接文件 ★

### 8.1 含义

- `ln` = link（链接）。链接分两类：**硬链接**与**软链接**。

### 8.2 命令格式

| 类型 | 命令 | 说明 |
| --- | --- | --- |
| 硬链接 | `ln 原文件 硬链接名` | 基于原文件创建 |
| 软链接 | `ln -s 原文件 软链接名` | -s = soft（software） |

### 8.3 硬链接（Hard Link）

- 类似于“复制”，但本质是在**磁盘同一个 inode（节点）**上增加了一个入口/文件名。
- 特点（课堂验证）：
  - 创建后 `ll` 显示：**文件类型仍为普通文件 `-`**；
  - **时间戳与原文件一致**；
  - **删除原文件，硬链接不受影响**（数据仍在磁盘节点中）；
  - 权限格式与原文件一致。

### 8.4 软链接（Soft Link / Symbolic Link）

- 类似 Windows **桌面快捷方式**：软件装在磁盘，桌面图标只是一个指向。
- 特点（课堂验证）：
  - 命令要加 `-s`；
  - 文件类型显示为 **`l`（链接文件）**，且后面有 `-> 原文件`；
  - **时间戳是创建时刻**；
  - **删除原文件后，软链接失效**（`ls`/`ll` 中显示红色/黑底，表示找不到目标）；
  - 类比：卸载软件后，桌面的快捷方式双击会提示“找不到”。

### 8.5 软链接 vs 硬链接对比表 ★

| 对比项 | 硬链接 | 软链接 |
| --- | --- | --- |
| 命令 | `ln 原 名` | `ln -s 原 名` |
| 文件类型（ll 首字符） | 普通文件 `-` | 链接文件 `l` |
| 时间戳 | 与原文件一致 | 创建时刻 |
| 指向 | 同一 inode 的不同入口 | 记录原文件的路径 |
| 删除原文件后 | 不受影响 | 失效（红/黑显示） |
| 类比 | 同一部电影的两个文件名 | Windows 快捷方式 |
| 跨文件系统 | 不可以 | 可以 |

## 九、网络命令简介（本节课讲义后续）★

由于课堂网络不稳定，本部分仅为简介，下次课会演示。

### 9.1 ifconfig：查看网络接口

- `ifconfig` 显示网卡信息，例如 `eth0`：
  - 网卡名称；
  - IP 地址；
  - RX（接收）和 TX（发送）的字节数；
  - 丢包率（联网正常时应为 0，网络不好会有丢包）。
- `lo` = loopback，**回环接口**（127.0.0.1），用于本机自测，平时配置 IP、开关网卡主要操作 `eth0` 等实际网卡。
- 常用：`sudo ifconfig eth0 192.168.1.10`（临时配置 IP）、`sudo ifconfig eth0 up/down`（开/关网卡）。

### 9.2 ping：测试网络连通

- 用途：测试某台机器在局域网中网络是否通畅。
- 前提：先配好 IP 才能 ping。
- 示例：`ping 192.168.1.1`；`Ctrl+C` 停止。

## 十、易错点复盘 ★

1. **cp 目录忘加 -r** → 报错；**mv 目录加了一堆参数** → 不需要。
2. **rm 目录忘加 -r** → 删不掉；`rm -rf` 又太危险，确认目标再删。
3. **权限混淆**：`r` 读、`w` 写、`x` 执行，别把读和写当成一回事。
4. **顺序固定**：权限永远是 u、g、o 三组，每组 rwx 顺序不变。
5. **数字换算**：r=4、w=2、x=1；`761` = u(7=rwx) g(6=rw-) o(1=--x)，顺序别写反。
6. **硬链接 ≠ 软链接**：硬链接删原文件还在；软链接（`ln -s`）删原文件就失效。
7. **改权限要 sudo**：权限不足时命令前面加 `sudo`。
8. **脚本要加 x**：`.sh` 脚本创建后不执行 `chmod +x` 无法运行。
9. **文件名不要重复**：创建链接或其他文件时避免同名冲突。

## 十一、课堂操作复盘（标准流程）

1. **移动文件**：`mv file1 /home/DIR2/` → 源目录 `ls` 确认没了 → 目标目录 `ls` 确认到了 → `ll` 看时间戳保留。
2. **移动目录**：`mv /home/DIR1 /home/DIR2/`（无需 -r）→ 两边 `ls` 验证。
3. **删除文件**：`rm file1`（有提示输入 `y`，权限不够加 `sudo`）。
4. **删除目录**：`sudo rm -r dir2` → `ls` 验证目录已删除。
5. **查看文件类型/权限**：`ll`，看第一列首字符（- / d / l）和后 9 位权限。
6. **改权限（字符方式）**：`sudo chmod g+w A`、`sudo chmod u-w A`。
7. **改权限（数字方式）**：`sudo chmod 761 hello.c` → `ll` 验证变 `rwx rw- --x`。
8. **创建硬链接**：`ln hello.c hhello.c` → `ll` 验证类型、时间戳一致；删原文件后硬链接仍在。
9. **创建软链接**：`ln -s hello.c shello.c` → `ll` 验证类型 `l` 且指向原文件；删原文件后软链接失效（红色/黑底）。
10. **网络检查**：`ifconfig` 看网卡/IP/丢包；`ping 目标IP` 测连通。

## 十二、复习自测题（含答案）

### 基础题

1. `mv` 与 `cp` 在操作**目录**时有什么区别？
2. `mv` 之后文件的时间戳为什么不会变？
3. `rm` 删除文件和删除目录分别怎么操作？
4. `ll` 第一列首字符 `-`、`d`、`l` 分别代表什么？
5. 权限中 `r`、`w`、`x` 分别是什么意思？
6. 权限 `rw-r--r--` 对应的数字是多少？`755` 对应什么？
7. `chmod` 有哪两种写法？各举一例。
8. `chmod 761 hello.c` 的含义是什么？
9. 硬链接和软链接有什么区别？
10. `ping` 命令的作用是什么？`ifconfig` 能查看哪些信息？

### 参考答案

1. 复制目录要 `cp -r`；移动目录 `mv` 直接可以，不用加 -r。
2. 移动只是改变文件位置，文件本身（含 inode 和时间戳）没有变；复制会生成新文件，时间戳为复制时刻。
3. 删文件 `rm 文件`；删目录 `rm -r 目录`（可加 -f 强制，慎用）。
4. `-` 普通文件；`d` 目录文件；`l` 链接文件。
5. r 可读、w 可写、x 可执行。
6. `rw-r--r--` = 644；`755` = u(rwx) g(r-x) o(r-x)。
7. 字符方式：`chmod u+x 文件`；数字方式：`chmod 755 文件`。
8. 所有者 rwx(7)、同组 rw-(6)、其他用户 --x(1)。
9. 硬链接是同一 inode 的另一个入口，时间戳一致、删原文件不受影响，类型显示普通文件；软链接类似快捷方式，类型为 l，删原文件后失效。
10. `ping` 测试网络连通性；`ifconfig` 查看网卡名称、IP、收发字节、丢包率等。

## 附录：本课命令速查表

| 命令 | 作用 | 常用形式 |
| --- | --- | --- |
| `mv 源 目标` | 移动/改名 | `mv file1 /home/DIR2/` |
| `rm 文件` | 删除文件 | `rm file1` |
| `rm -r 目录` | 递归删除目录 | `sudo rm -r dir2` |
| `rm -rf 目录` | 强制递归删除（危险） | 慎用 |
| `ln 原 链接名` | 创建硬链接 | `ln hello.c hhello.c` |
| `ln -s 原 链接名` | 创建软链接 | `ln -s hello.c shello.c` |
| `chmod 字符 文件` | 符号方式改权限 | `sudo chmod u+x a.sh` |
| `chmod 数字 文件` | 数字方式改权限 | `sudo chmod 761 hello.c` |
| `ifconfig` | 查看/配置网卡 | `sudo ifconfig eth0` |
| `ping 地址` | 测试网络连通 | `ping 192.168.1.1` |
| `ll` / `ls -l` | 详细信息（类型+权限+时间） | `ll` |

## 补充说明（授课信息）

- 老师已将“第一章课件”和本课讲义发布到**智慧树平台**，可以课后查看加深理解。
- 课后作业截止时间为 **9 月 23 日 0:00**（即 9 月 22 日半夜 12 点），注意不要晚交。